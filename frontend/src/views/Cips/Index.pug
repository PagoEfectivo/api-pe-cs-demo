include ../_mixins/mixin.pug


| @{
| ViewData["Title"] = "Página de Inicio";
| Layout = "~/Views/Shared/_Layout.cshtml"; 
| }
div.header-custom
  button#sidebarCollapseTitle.btn.btn-outline-warning(type='button')
    div.menu-icon
    div.menu-icon
    div.menu-icon
  span.header-title
    | CONFIGURACION CIP
div.content-main               
  .row
      .col-sm-6
        .form-group
          label(for='serviceId') ID de servicio:
          input#serviceId.form-control(type='text')
  .row
      .col-sm-6
        .form-group
          label(for='accessKey') Access key:
          input#accessKey.form-control(type='password')
  .row
  
      .col-sm-6
        .form-group
          label(for='secretKey') Secret key:
          input#secretKey.form-control(type='password')
  button.btn.btn-outline-secondary(type='submit', onclick='credentialRegister()') Registrar
  br

script.
  function credentialRegister(){
    var serviceId = document.getElementById("serviceId");
    var accessKey = document.getElementById("accessKey");
    var secretKey = document.getElementById("secretKey");
    setCookie("serviceId", serviceId.value);
    setCookie("accessKey", accessKey.value);
    setCookie("secretKey", secretKey.value);
  }
style(type='text/css').
  @media print { body { -webkit-print-color-adjust: exact; } }


